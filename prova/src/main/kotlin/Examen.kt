
/**
 * Determina si una persona és d'edat legal segons la seva edat. Atenció
 * l'edat no pot ser inferior a 1. En cas de que així sigui escribiu la
 * següent línia de codi 
 * 
 **     throw InvalidParameterException("L'edat no pot ser negativa")
 *
 * @param edat L'edat de la persona.
 * @return Retorna `true` si la persona és d'edat legal (18 o més anys),
 *     `false` en cas contrari.
 */
fun comprobarMajorEdat(edat: Int): Boolean {
    throw NotImplementedError("La funció encara no està implementada")
}

/**
 * Exercici 2. Realitza un bucle for perquè iteri de l'1 al max. Introdueix
 * en una llista només els nombres que siguin múltiples de 3. Utilitza una
 * condició dins del bucle per determinar si un nombre és múltiple de 3. .
 */
fun multipleDeTres(max: Int): List<Int> {
    val llista = mutableListOf<Int>()

    throw NotImplementedError("La funció encara no està implementada")
}

/**
 * Donada una llista de nombres enters. Realitza les dues funcions
 * següents: una per calcular la suma dels nombres de la llista i una altra
 * per calcular la mitjana.
 *
 * Atenció, si la llista està buida al moment de realitzar la mitjana has
 * d'afegir la següent línia:
 *
 * throw ArithmeticException("No es pot dividir per 0")
 *
 * @param numeros List<Int>
 * @return Int
 */

fun calcularSuma(numeros: List<Int>): Int {
    var suma = 0
    throw NotImplementedError("La funció encara no està implementada")
}

fun calcularMitjana(numeros: List<Int>): Double {
    throw NotImplementedError("La funció encara no està implementada")
}

/**
 * Crea una funció en Kotlin que rebi una llista de nombres enters i
 * retorni una nova llista on cada nombre parell sigui reemplaçat pel
 * doble del seu valor i cada nombre senar sigui reemplaçat pel triple.
 *
 * @param numeros List<Int>
 * @return List<Int>
 */

fun transformarNumeros(numeros: List<Int>): List<Int> {
    val llistaTransformada = mutableListOf<Int>()
    throw NotImplementedError("La funció encara no està implementada")
}

fun filtrarMenorsQueMax(numeros: List<Int>, max: Int): List<Int> {
    throw NotImplementedError("La funció encara no està implementada")
}


/**
 * Donada una paraula retorna un CharArray on cada lletra correspongui a un
 * _ Per exemple la paraula hola serà `____`
 * Atenció, tots aquells caràcters que no corresponguin a una lletra de l'abecedari s'han de mantenir
 * Per exemple la paraula hola! serà ____!
 *
 * @param paraula String
 * @return CharArray
 */
fun convertirParaula(paraula: String): CharArray {
    throw NotImplementedError("La funció encara no està implementada")

}

fun main() {

    /** TODO Fica un println amb el teu nom */

}